You are updating an existing web app called "Road to Glory 2026". Do NOT rebuild from scratch.

GOAL:
Create a premium "Champion Reveal" section that replaces the basic winner display with a cinematic experience.

---

CHAMPION DISPLAY:

When the final winner is selected:

- Show a centered "Champion Card" including:
  - Team flag (large)
  - Team name (bold, big text)
  - Label: "Champion"
  - Animated trophy above or behind

Example layout:

        [ Trophy Animation ]
        Champion
        🇦🇷 Argentina

---

TROPHY INTEGRATION:

- Use a high-quality gold trophy image or SVG
- Place it:
  - Above the team OR
  - Slightly behind with glow effect

---

ANIMATION SEQUENCE (VERY IMPORTANT):

Trigger when winner is selected:

1. Fade out previous bracket focus slightly
2. Animate champion card:
   - Fade in
   - Scale from 0.8 → 1
   - Smooth ease-out

3. Trophy animation:
   - Drop in from top OR scale up
   - Add glow pulse

4. Add background effect:
   - Radial gold glow behind champion
   - Optional particles/confetti

---

CSS ANIMATION EXAMPLE:

.champion-card {
  animation: reveal 0.6s ease-out;
}

@keyframes reveal {
  from {
    opacity: 0;
    transform: scale(0.8) translateY(20px);
  }
  to {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}

.glow {
  box-shadow: 0 0 40px rgba(212,175,55,0.6);
}

---

VISUAL EFFECTS:

- Add gold glow behind champion
- Add subtle floating animation to trophy
- Add confetti or particle burst (lightweight)

---

RESPONSIVENESS:

- On mobile:
  - Stack vertically
  - Keep trophy smaller but visible
  - Ensure text is readable

---

IMAGE EXPORT (IMPORTANT):

- Ensure champion card + trophy are included in html2canvas capture
- Keep centered layout so exported image looks clean
- Maintain high resolution

---

RESULT:

When a winner is selected, the app should feel like a real tournament victory moment, with a premium animated trophy, glowing champion card, and strong visual focus.